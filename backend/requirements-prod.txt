# Slim production requirements for Cloudflare Containers.
# Excludes: torch, sentence-transformers, streamlit*, matplotlib, pytest*,
# chromadb (prod uses pgvector via src/storage/vector_store.py).

# Agent pipeline
langchain>=0.3.0
langchain-community>=0.3.0
langchain-openai>=0.3.0
langgraph>=0.4.0
google-genai>=1.70.0
openai>=1.50.0

# Ingestion
pymupdf>=1.25.0
Pillow>=11.0.0
python-docx>=1.1.0

# Hybrid retrieval
rank-bm25>=0.2.2

# Reranking (API-only)
cohere>=5.11.0

# API layer
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
sse-starlette>=2.1.0
pydantic>=2.9.0
pydantic-settings>=2.5.0
httpx>=0.27.0
python-multipart>=0.0.12

# Auth (Clerk JWT verification)
pyjwt[crypto]>=2.9.0

# Storage
sqlalchemy>=2.0.30
psycopg[binary]>=3.2.0
aiosqlite>=0.20.0

# AWS S3 SDK (R2 is S3-compatible)
boto3>=1.35.0

# Misc
python-dotenv>=1.1.0
